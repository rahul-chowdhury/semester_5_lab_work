#include<stdio.h>
#include<stdlib.h>
#include <time.h>
int main(int argc,char *argv[])
{
	//FILE *file_read=fopen(args[0],"r"),*file_write=fopen(args[1],"w");
	
	int a,b;
	//scanf(file_read,"%d %d",&a,&b);
	//printf(file_write,"a=%d b=%d sum=%d",a,b,(a+b));
	//close(file_read);
	//close(file_write);
	scanf("%d %d",&a,&b);
	printf("a=%d b=%d sum=%d\n",a,b,(a+b));
	return 0;
}
	
	
